{
  "$id": "https://opendatafabric.org/schemas/config/v1alpha1/ValueRef",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Reference to a value within a `VariableSet` or a `SecretSet`.",
  "format": "struct-or-string",
  "oneOf": [
    {
      "type": "string"
    },
    {
      "type": "object",
      "codegen": {
        "rust": {
          "dtoType": "SecretRef"
        }
      },
      "required": [
        "type"
      ],
      "properties": {
        "account": {
          "$ref": "https://opendatafabric.org/schemas/auth/v1alpha1/AccountRef",
          "description": "Reference to an account that owns the `VariableSet` or the `SecretSet`.",
          "unevaluatedProperties": false
        },
        "type": {
          "type": "string",
          "format": "resource-type-ref",
          "examples": [
            "SecretSet",
            "VariableSet",
            "https://opendatafabric.org/config/v1/SecretSet"
          ],
          "description": "Short type name or full type URI of the target resource."
        },
        "id": {
          "type": "string",
          "format": "resource-id",
          "description": "ID of a resource."
        },
        "name": {
          "type": "string",
          "format": "resource-name",
          "description": "Name of a resource."
        },
        "path": {
          "type": "string",
          "examples": [
            "port",
            "postgres.schemaName"
          ],
          "description": "JSON path to a value within a `VariableSet` or a `SecretSet`."
        }
      }
    }
  ]
}