{
  "$id": "https://opendatafabric.org/schemas/dataset/v1alpha1/Watermark.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Represents a watermark in the event stream.",
  "type": "object",
  "required": [
    "systemTime",
    "eventTime"
  ],
  "properties": {
    "systemTime": {
      "type": "string",
      "format": "date-time",
      "description": "Moment in processing time when watermark was emitted."
    },
    "eventTime": {
      "type": "string",
      "format": "date-time",
      "description": "Moment in event time which watermark has reached."
    }
  }
}
