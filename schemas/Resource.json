{
  "$id": "http://open-data-fabric.github.com/schemas/Resource",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Top-level container for resources that specifies the type and version of the resource it's specifying and carries identity, ownership, and status information.",
  "type": "object",
  "unevaluatedProperties": false,
  "required": [
    "$schema",
    "headers",
    "spec"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "Defines a bounded context that this resource belongs to. Context is usually composed of a domain and a version e.g. `datasets.opendatafabric.org/v1`."
    },
    "headers": {
      "$ref": "/schemas/ResourceHeaders",
      "description": "Container for identity and ownership information of a resource."
    },
    "spec": {
      "type": "object",
      "format": "fragment",
      "description": "Specifies the desired state of a resource."
    },
    "status": {
      "$ref": "/schemas/ResourceStatus",
      "description": "Resource lifecycle and reconciliation inforamtion."
    }
  }
}
