{
  "$id": "http://open-data-fabric.github.com/schemas/ExecuteQuery",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Indicates that derivative transformation has been performed.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "inputSlices"
  ],
  "properties": {
    "inputSlices": {
      "type": "array",
      "items": {
        "$ref": "/schemas/InputSlice"
      },
      "description": "Defines inputs used in this transaction. Slices corresponding to every input must be present."
    },
    "inputCheckpoint": {
      "type": "string",
      "format": "multihash",
      "description": "Hash of the checkpoint file used to restore transformation state, if any."
    },
    "outputData": {
      "$ref": "/schemas/DataSlice",
      "description": "Describes output data written during this transaction, if any."
    },
    "outputCheckpoint": {
      "$ref": "/schemas/Checkpoint",
      "description": "Describes checkpoint written during this transaction, if any. If an engine operation resulted in no updates to the checkpoint, but checkpoint is still relevant for subsequent runs - a hash of the previous checkpoint should be specified."
    },
    "outputWatermark": {
      "type": "string",
      "format": "date-time",
      "description": "Last watermark of the output data stream, if any. Initial blocks may not have watermarks, but once watermark is set - all subsequent blocks should either carry the same watermark or specify a new (greater) one."
    }
  }
}