{
  "$id": "http://open-data-fabric.github.com/schemas/DerivativeInput.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Defines an input dataset used in derivative source and its configuration",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Identifier of the input dataset."
    }
  }
}