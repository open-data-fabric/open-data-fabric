{
  "$id": "http://open-data-fabric.github.com/schemas/ResourcePhase",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Represents the lifecycle stage of a resource.",
  "type": "string",
  "enum": [
    "Pending",
    "Reconciling",
    "Ready",
    "Failed"
  ]
}
