{
  "$id": "https://opendatafabric.org/schemas/resources/v1alpha1/Resource",
  "$schema": "https://opendatafabric.org/schemas/meta/v1alpha1/Manifest",
  "description": "Top-level container for canonical representation of a resource that specifies the type and version of the resource, carries identity, ownership, and status information.",
  "type": "object",
  "unevaluatedProperties": false,
  "required": [
    "$schema",
    "headers",
    "spec",
    "status"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "type-uri",
      "description": "Identifies the controlling entity, a bounded context that this resource belongs to, and the version. Url should follow the pattern `{base-url}/{context}/{version}/{name}.json` e.g. `https://opendatafabric.org/schemas/datasets/v1/Dataset.json`."
    },
    "headers": {
      "$ref": "https://opendatafabric.org/schemas/resources/v1alpha1/ResourceHeaders",
      "description": "Container for identity and ownership information of a resource.",
      "unevaluatedProperties": false
    },
    "spec": {
      "type": "object",
      "format": "fragment",
      "description": "Specifies the desired state of a resource."
    },
    "status": {
      "$ref": "https://opendatafabric.org/schemas/resources/v1alpha1/ResourceStatus",
      "description": "Resource lifecycle and reconciliation information.",
      "unevaluatedProperties": false
    }
  }
}
