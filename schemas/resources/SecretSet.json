{
  "$id": "http://open-data-fabric.github.com/schemas/SecretSet",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Defines a set of secrets stored and managed by the ODF node.",
  "type": "object",
  "unevaluatedProperties": false,
  "required": [
    "$schema",
    "headers",
    "spec"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "const": "http://open-data-fabric.github.com/schemas/SecretSet"
    },
    "headers": {
      "$ref": "/schemas/ResourceHeaders",
      "description": "Container for identity and ownership information of a resource.",
      "unevaluatedProperties": false
    },
    "spec": {
      "$ref": "/schemas/SecretSetSpec",
      "description": "Specifies the desired state of the secret set.",
      "unevaluatedProperties": false
    },
    "status": {
      "$ref": "/schemas/ResourceStatus",
      "description": "Resource lifecycle and reconciliation information.",
      "unevaluatedProperties": false
    }
  }
}
