{
  "$id": "http://open-data-fabric.github.com/schemas/OutputSlice",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Describes a slice of output data added to a dataset or produced via transformation",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "dataLogicalHash",
    "dataInterval"
  ],
  "properties": {
    "dataLogicalHash": {
      "type": "string",
      "format": "sha3-256",
      "description": "Logical hash sum of the data in this slice"
    },
    "dataInterval": {
      "$ref": "/schemas/OffsetInterval",
      "description": "Data slice produced by the transaction"
    }
  }
}