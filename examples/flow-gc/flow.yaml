# $schema: https://opendatafabric.org/schemas/flow/v1alpha1/Flow
$schema: https://opendatafabric.org/schemas/flow/v1alpha1/Flow
headers:
  name: gc-datasets
spec:
  target: Dataset:%
  triggers:
    - kind: Schedule
      cron: "@daily"
  tasks:
    - kind: GarbageCollection
